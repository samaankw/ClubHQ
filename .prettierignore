node_modules/
.expo/
dist/
web-build/
coverage/
android/
ios/
types/database.types.ts
supabase/functions/
supabase/.temp/

# Incremental adoption: Prettier was added in Phase 1 against an existing,
# unformatted codebase. Reformatting all of it in the same PR that adds the
# tool would touch ~44 files with zero logic changes -- exactly the kind of
# unrelated mass churn a phase-scoped diff is supposed to avoid. The specific
# pre-existing files below are ignored *for now*; the intent is to shrink
# this list as files get genuinely touched in later phases (format them
# as you go), not to leave it here permanently. New files added after
# Phase 1 are NOT covered by this list and are checked by default.
app/index.tsx
app/(auth)/legal-terms.tsx
app/(auth)/privacy.tsx
app/legal/privacy.tsx
app/legal/terms.tsx
lib/alertCompat.ts
lib/AuthProvider.tsx
lib/index.tsx
lib/notifications.ts
types/db.ts
README.md
docs/marketing/
